<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.scoula.boards.mapper.BoardMapper">

    <select id="getList" resultType="org.scoula.boards.domain.BoardVO">
<![CDATA[
        select * from tbl_board
        order by no desc
        ]]>
</select>


</mapper>
